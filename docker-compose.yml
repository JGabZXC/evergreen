version: "3.8"
services:
  frontend:
    build:
      context: ./frontend
      target: development
    container_name: evergreen_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    env_file:
      - ./frontend/.env
  backend:
    build: ./backend
    container_name: evergreen_backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    depends_on:
      - mongo
  mongo:
    image: mongo:6
    container_name: evergreen_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
